import { createTheme } from '@stitches/react';

export const Dark = createTheme({
  colors: {
    primary: '#010207', // preto
    secondary: '#FFE919', // amarelo
    tertiary: '#ffffff', // 
  },
}) 