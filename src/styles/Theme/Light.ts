import { createTheme } from '@stitches/react';

export const Light = createTheme({
  colors: {
    primary: '#ffffff', // branco
    secondary: '#010207', // preto
    tertiary: '#FFE919' // amarelo
  },
}) 